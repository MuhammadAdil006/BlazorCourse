<h3>ChildPropertiesTriggerComponent</h3>
<p>@message</p>
@code {
    public String message { get; set; } = "default text";
    //it will not change its state so we have to call funciton
    public void Show()
    {
        message = "text changes hohoohohohh";
        StateHasChanged();
    }

}
