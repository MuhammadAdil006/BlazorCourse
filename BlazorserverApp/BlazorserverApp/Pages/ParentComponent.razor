@page "/pc"
<div>
    <h3>ParentComponent</h3>
    <ChildComponent Title="i am passing some title from parent to child"></ChildComponent>
    <a href="/RP/somtitle/34">route paramter component with paramters</a>
    <a hreg="/RP">without paramter routing</a>
    <CascadingValue Value="@Name" Name="Name" IsFixed="true">
        <CascadingValue Value="@Id" Name="Id">
            @*now there may be pergformance issues as asp.net will keep an eye on these cascade values*@
            @*to avoid add is fiexed true*@
            @*that will tell this will not change*@
            <CascadingCHildComponent></CascadingCHildComponent>
        </CascadingValue>
         
    </CascadingValue>
    <p>
    clicking on this button wil change text in child i mean it willtrigger properties meth0d on child</p>
    <button @onclick="trigger">Click to trigger child properties</button>
    
<ChildPropertiesTriggerComponent @ref="c1"></ChildPropertiesTriggerComponent>
    
</div>
@*now we want to send some data to its child and then also grand child component*@
@*this can be done using cascading parameter*@
@code {
    private ChildPropertiesTriggerComponent c1;
    [Parameter]
    public String? Name { get; set; } = "User Name adil";
    public int? Id { get; set; } = 12;
    public void trigger()
    {
        c1.Show();

    }

}
